# M2 experiments

`benchmark/development_queries.json` is a small, manually curated development benchmark. `results/benchmark.json` is generated locally and records timings, metrics, index metadata, and peak RSS. It is not evidence of production retrieval quality.
